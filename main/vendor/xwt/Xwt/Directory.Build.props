<Project>
  <!-- MonoDevelop local patch: use the repository props (net10.0, ADR 0003) instead of upstream's net461;net40. -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
</Project>
