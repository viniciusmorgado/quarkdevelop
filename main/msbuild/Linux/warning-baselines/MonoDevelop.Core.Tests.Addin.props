<Project>
  <!-- Generated by scripts/warnings-baseline.sh — legacy warning IDs of MonoDevelop.Core.Tests.Addin that stay warnings.
       Any other warning ID is an error (constitution V). Shrink this list over time. -->
  <PropertyGroup>
    <WarningsNotAsErrors>$(WarningsNotAsErrors);CA1852</WarningsNotAsErrors>
  </PropertyGroup>
</Project>
