<Project Sdk="Microsoft.NET.Sdk">
  <!-- Converted from the legacy csproj by scripts/convert-legacy-csproj.py (ADR 0003); GTK3 port (T094). -->
  <PropertyGroup>
    <AssemblyName>MonoDevelop.RegexToolkit</AssemblyName>
    <RootNamespace>MonoDevelop.RegexToolkit</RootNamespace>
    <AddinBuildDir>MonoDevelop.RegexToolkit</AddinBuildDir>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="AssemblyInfo.cs" />
    <Compile Include="MonoDevelop.RegexToolkit/Commands.cs" />
    <Compile Include="MonoDevelop.RegexToolkit/RegexToolkitWidget.cs" />
    <Compile Include="MonoDevelop.RegexToolkit/ElementHelpWidget.cs" />
    <Compile Include="AddinInfo.cs" />
    <Compile Include="MonoDevelop.RegexToolkit/ValidateActionCodeRefactoringProvider.cs" />
    <!-- Stetic output, frozen as hand-maintained GTK3 code (ADR 0011); the project has no gui.stetic. -->
    <Compile Include="Gui/MonoDevelop.RegexToolkit.ElementHelpWidget.cs" />
    <Compile Include="Gui/MonoDevelop.RegexToolkit.RegexToolkitWidget.cs" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="MonoDevelop.RegexToolkit.addin.xml" />
    <EmbeddedResource Include="RegexElements.xml" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../core/MonoDevelop.Core/MonoDevelop.Core.csproj" Private="false" />
    <ProjectReference Include="../../core/MonoDevelop.Ide/MonoDevelop.Ide.csproj" Private="false" />
    <!-- Dropped: MonoDevelop.DesignerSupport, System.Web and System.Web.Services (no code uses them). -->
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Mono.Addins" />
  </ItemGroup>
</Project>
