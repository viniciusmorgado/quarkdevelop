<Project>
  <!--
    The unit testing samples are *user* test projects (FR-011). They must not inherit the repository's build
    configuration (main/Directory.Build.targets), so this file intentionally stops the Directory.Build.* chain.
  -->
</Project>
